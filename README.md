# Monster Type Game

A small typing game built with React and Vite. You type a magic word before the monster reaches you. **[Motion](https://motion.dev)** (`motion/react`) handles the UI animations (monster approach, spell flash, wizard feedback, letter micro-interactions).

## Run locally

```bash
npm install
npm run dev
```
